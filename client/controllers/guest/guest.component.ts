import { Component, Input } from '@angular/core';
import { Guest } from './guest';

@Component({
  selector: 'guest-item',
  templateUrl: 'controllers/guest/guest.component.html',
  styleUrls: ['controllers/guest/guest.component.css']
})
export default class GuestComponent {
}